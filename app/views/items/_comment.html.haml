%li.comment
  = link_to '#', class: 'comment__user' do
    = icon('fas', 'user-circle', class: "comment__user__icon")
    .comment__user__nickname
      = comment.user.nickname
    - if comment.user == @item.seller
      .comment__user__seller 出品者
  .comment__body
    .comment__body__text
      = simple_format(comment.text)
    .comment__body__time
      = icon('far', 'clock', class: 'comment__body__time__icon')
      = comment.created_at.strftime("%Y/%m/%d(%a) %H:%M")